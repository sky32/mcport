// Core LSP packages are installed on demand by Desktop into userData. The
// application build must remain independent of those optional downloads.
console.log('Core LSP packages are managed on demand by Desktop settings.');
